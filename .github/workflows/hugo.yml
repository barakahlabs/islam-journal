name: Deploy Hugo site to Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      HUGO_VERSION: 0.164.0
    steps:
      - name: Install Hugo CLI
        run: |
          wget -O ${{ runner.temp }}/hugo.deb https://github.com/gohugoio/hugo/releases/download/v${HUGO_VERSION}/hugo_extended_${HUGO_VERSION}_linux-amd64.deb \
          && sudo dpkg -i ${{ runner.temp }}/hugo.deb
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v5
      - name: Build islamic-journal
        env:
          HUGO_ENVIRONMENT: production
          HUGO_ENV: production
        working-directory: sites/islamic-journal
        run: hugo --gc --minify --destination ../../dist/islamic-journal
      - name: Build poems
        env:
          HUGO_ENVIRONMENT: production
          HUGO_ENV: production
        working-directory: sites/poems
        run: hugo --gc --minify --destination ../../dist/poems
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install book dependencies
        working-directory: sites/book
        run: npm install
      - name: Build book
        env:
          HUGO_ENVIRONMENT: production
          HUGO_ENV: production
        working-directory: sites/book
        run: npx hugo --gc --minify --destination ../../dist/book
      - name: Add landing page
        run: |
          mkdir -p dist
          cp landing/index.html dist/index.html
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
